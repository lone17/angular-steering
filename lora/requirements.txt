# LoRA training pipeline dependencies
# Install with: pip install -r lora/requirements.txt

# Core ML
torch>=2.0.0
transformers>=4.40.0
peft>=0.10.0          # LoRA via PEFT

# Utilities
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
datasets>=2.0.0       # for data loading helpers
vllm>=0.11.0
scikit-learn>=1.3.0
